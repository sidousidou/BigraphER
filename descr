JSON codec for bigraphical structures

big_json is a codec to encode and decode bigraphical structures such as bigraphs
and reaction rules to the JSON data format. The library also provides a JSON
interface to the matching engine of the bigraph library.

big_json is distributed under the BSD-3 license.