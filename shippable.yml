build_environment: Ubuntu 13.04

language: java

before_install: 
 - sudo add-apt-repository -y ppa:avsm/ocaml41+opam11
 - sudo apt-get update -qq 
 - sudo apt-get install -qq ocaml opam graphviz

script: 
 - ./shippable-ci.sh